version: "3.9"
services:
  web:
    build: .
    container_name: btc-mini
    ports:
      - "8000:8000"
    environment:
      - DB_PATH=/app/data/btcmini.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
